{% if image %}
  <div class="top-image mb-3">
    <img src="{{ image.src }}" alt="{{ imageAlt }}"/>
    {% if image.credit %}
      <p class="credit">
  Image credit
  <a href="{{ image.credit.url }}">{{ image.credit.author }}</a>
      </p>
    {% endif %}
  </div>
{% endif %}
